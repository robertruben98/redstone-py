# redstone-py

Python client for the RedStone oracle API (modular pull price feeds). Sync + async, Pydantic v2.

## Install

```bash
pip install redstone-py
```

The distribution is `redstone-py`; the import package is `redstone`.

## Quick start

```python
import redstone
```

See the [API reference](api.md) for the full surface.
